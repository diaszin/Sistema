<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SD&Cia - A Melhor loja de ferramentas do Brasil</title>
    <!-- Boostrap Links e Scripts ... -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.1/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/redirecionarCompra.js" defer></script>
</head>
<body>
  <nav class="navbar navbar-expand-lg text-bg-secondary">
    <div class="container-fluid">
        <a class="navbar-brand active" href="/">SD&Cia</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <ul class="navbar-nav mr-auto">

          <% if( clienteLogado == true ) {%>
            <li class="nav-item">
              <a class="nav-link" href="/cliente/conta">Minha conta<span class="sr-only">(Cliente Logado)</span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-danger" href="/cliente/conta/sair">Sair<span class="sr-only">(Cliente Logado)</span></a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/login/cliente">Sou Cliente<span class="sr-only">(Login Cliente)</span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/login/funcionario">Sou Funcionário<span class="sr-only">(Login Funcionário)</span></a>
            </li>
          <% } %>

        </ul>
    </div>
</nav>
      <main class="d-flex flex-column justify-content-center align-content-center">
        <% if(data.length>0)
        {
        %>
        <!-- Titulo da Página -->
        <section class="container text-center">
            <div class="row row-cols-4">
                <% for(let i=0;i< data.length; i++){ %>
                    <div class="col my-5 my-1 d-flex flex-column shadow pb-3" style="border-radius: 1.4rem;">
                        <img src="assets/imagem_produto.svg" alt="#">
                        <h4 class="text-truncate">
                            <%=data[i].nome%>
                        </h4>
                        <p>R$ <%=data[i].valor%>
                        </p>
                        <input type="hidden" name="id" value="">
                        <button class="btn btn-success comprar" style="border-radius: 0px;" data-id="<%=data[i].id%>">Comprar Agora</button>
                        <button class="btn btn-outline-primary carrinho" style="border-radius: 0px;"><b>Carrinho
                                <bdi>+</bdi></b></button>
                    </div>
                <% } %>
            </div>
        </section>
        <% } %>
      </main>
</body>
</html>